extends: ./electron-builder.common.yml

# Generic URL = download links inside *.yml (must match OSS custom domain + prefix).
# Actual upload: `zx scripts/upload-release-to-oss.mjs` (app-builder S3 publish breaks on Aliyun OSS).
publish:
  - provider: generic
    url: https://bucketclaw.1m1sj.xin/latest
    useMultipleRangeRequest: false
